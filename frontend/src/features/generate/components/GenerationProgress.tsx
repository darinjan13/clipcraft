import { motion } from 'framer-motion';
import { CheckCircle2, LoaderCircle, Sparkles } from 'lucide-react';
import { Panel } from '@/components/ui/Panel';
import { Progress } from '@/components/ui/Progress';

export function GenerationProgress({ progress, title, active }: { progress: number; title: string; active: boolean }) { const done = progress >= 100; return <Panel className="flex h-full min-h-[480px] flex-col justify-between overflow-hidden p-7" role="status" aria-live="polite" aria-busy={active && !done}><div><div className="flex items-center justify-between"><p className="eyebrow">Render preview</p><span className="font-mono text-[10px] text-white/35">{done ? 'COMPLETE' : active ? 'LIVE' : 'READY'}</span></div><div className="relative mx-auto mt-10 grid aspect-[9/14] max-h-[360px] w-full max-w-[220px] place-items-center overflow-hidden rounded-2xl border border-white/10 bg-aurora bg-[#15131c] shadow-glow"><div className="absolute inset-0 bg-gradient-to-b from-violet-400/10 via-transparent to-blue-500/10" /><div className="relative text-center"><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-white/10 text-violet-200">{done ? <CheckCircle2 className="size-6" /> : active ? <LoaderCircle className="size-6 animate-spin" /> : <Sparkles className="size-6" />}</span><p className="mt-4 max-w-[150px] text-xs leading-5 text-white/55">{done ? 'Your video is ready to preview.' : active ? 'Composing your visual story...' : 'Your preview will appear here after generation starts.'}</p></div></div></div><div className="mt-8"><div className="mb-3 flex items-center justify-between text-xs"><span className="text-white/60">{title || 'Untitled creation'}</span><span className="font-mono text-white/45">{done ? '100%' : active ? 'In progress' : '0%'}</span></div><Progress value={progress} indeterminate={active && !done} /></div></Panel>; }
