import { ArrowUpRight, Clock3, Play } from 'lucide-react';
import { Link } from 'react-router-dom';
import { Badge } from '@/components/ui/Badge';
import { Progress } from '@/components/ui/Progress';
import type { Video } from '@/features/videos/types';
import { formatRelativeDate } from '@/lib/utils';

export function VideoCard({ video }: { video: Video }) { const rendering = video.status === 'rendering' || video.status === 'queued'; return <Link to={`/library/${video.id}`} className="group block overflow-hidden rounded-2xl border border-white/[.08] bg-white/[.035] transition hover:-translate-y-0.5 hover:border-violet-300/25 hover:bg-white/[.055]"><div className="relative aspect-[16/10] overflow-hidden bg-[#15131c]" style={{ background: `linear-gradient(135deg, ${video.thumbnail}, #111116)` }}><div className="absolute inset-0 bg-aurora opacity-80" /><div className="absolute inset-0 bg-gradient-to-t from-black/70 via-transparent to-transparent" /><div className="absolute left-4 top-4"><Badge tone={video.status === 'completed' ? 'green' : 'violet'}>{video.status}</Badge></div><span className="absolute bottom-4 left-4 grid size-9 place-items-center rounded-full border border-white/20 bg-black/30 text-white backdrop-blur transition group-hover:scale-110 group-hover:bg-violet-400/30"><Play className="ml-0.5 size-4 fill-current" /></span><span className="absolute bottom-4 right-4 rounded-md bg-black/40 px-2 py-1 font-mono text-[10px] text-white/70 backdrop-blur">{video.duration}s</span></div><div className="p-4"><div className="flex items-start justify-between gap-3"><div><h3 className="text-sm font-semibold text-white/85">{video.title}</h3><p className="mt-1 line-clamp-1 text-xs text-white/35">{video.prompt}</p></div><ArrowUpRight className="size-4 shrink-0 text-white/25 transition group-hover:text-violet-200" /></div>{rendering && <div className="mt-4"><div className="mb-2 flex justify-between font-mono text-[10px] text-white/35"><span>Rendering</span><span>{video.progress}%</span></div><Progress value={video.progress} /></div>}<div className="mt-4 flex items-center gap-2 font-mono text-[10px] uppercase tracking-[.12em] text-white/25"><Clock3 className="size-3" />{formatRelativeDate(video.createdAt)}<span className="ml-auto">{video.aspectRatio}</span></div></div></Link>; }
